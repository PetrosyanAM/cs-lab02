#include <iostream>

using namespace std;

cout << "Enter A and B:";
int a, b;
cin >> a >> b;
cout << "A + B = "<< a+b<<'\n'
     << "A - B = "<< a-b<<'\n'
     << "A * B = "<< a*b<<'\n'
     << "A / B = "<< a/b<<'\n';
 HEAD
    if (a>b)
        cout << "min = " << b << '\n';
    if (a<b)
        cout << "min = " << a << '\n';

     if (a>b)
         cout << "max = " << a << '\n';
     if (a<b)
         cout << "max = " << b << '\n';


}
 44cad0fe696434a0ab2caee256d42cef835eab29
